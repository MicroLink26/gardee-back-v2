version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "08:00"
      timezone: Europe/Paris
    open-pull-requests-limit: 5
    labels: [dependencies]
    ignore:
      # Ne pas mettre à jour mongoose 8→9 sans migration volontaire
      - dependency-name: mongoose
        update-types: [version-update:semver-major]
